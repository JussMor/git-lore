# git-lore explain

## Description

Provides an explanation of why a particular piece of code exists, relying entirely on the "Lore" (rules, accepted assumptions, previous decisions) bound to that specific file and line.

## Usage

`git-lore explain --file <PathBuf> [--cursor-line <usize>]`
