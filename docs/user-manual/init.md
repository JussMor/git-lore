# git-lore init

## Description

Initializes a new Git-Lore workspace.

It creates the `.lore` directory at the specified path to store atoms, checkpoints, and internal configuration.

## Usage

`git-lore init [PATH]`

- `[PATH]`: Optional. The repository root or directory where you want to scaffold the workspace. Defaults to `.` (current directory).
