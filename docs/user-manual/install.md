# git-lore install

## Description

Properly straps Git-Lore into your local Git repository.

Installs local Git Hooks (so that PR validation works natively) and registers the custom merge-driver for Lore collision reconciliation (so branches don't overwrite JSON data blindly).

## Usage

`git-lore install [PATH]`
